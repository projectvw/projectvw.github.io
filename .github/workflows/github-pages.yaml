name: Build and deploy jekyll site

on:
  push:
    branches:
      - dev

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Reusable Jekyll Build and Deploy
      uses: https://github.com/ChrisHammond/christoc-jekyll-builddeploy/.github/actions/build-and-deploy-jekyll@v0.0.1
      with:
        ruby_version: '3.2.2'
        jekyll_build_opts: ''
